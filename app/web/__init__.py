"""웹 UI — `serve`가 띄우는 화면 (ARCHITECTURE.md 8장 / 12.9).

**v0.4의 FastAPI + React를 되살린 것이 아니다.** 그때 걷어낸 것은 *파이프라인을
캔버스로 편집하는 앱*이었고, 여기 있는 것은 **이미 있는 명령을 부르고 결과를
띄우는 서버 렌더 화면**이다. 그래서 Node·pnpm·빌드 단계가 없다 (부록 C 19번).

★ **실행은 `app/service.py`를 통해서만 한다.** 화면이 직접 엔진을 부르기 시작하면
"버튼으로 돌린 것과 터미널로 돌린 것이 다른 일을 하는" 상태가 되고, 그건 규칙
11·13이 지키려던 것을 화면 쪽으로 우회하는 것이다.
"""

from app.web.app import create_app

__all__ = ["create_app"]
